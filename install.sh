#!/usr/bin/env bash

echo "sudo curl -L https://raw.githubusercontent.com/richarddewit/sly/master/sly -o /usr/local/bin/sly"
sudo curl -L https://raw.githubusercontent.com/richarddewit/sly/master/sly -o /usr/local/bin/sly
echo "sudo chmod +x /usr/local/bin/sly"
sudo chmod +x /usr/local/bin/sly
